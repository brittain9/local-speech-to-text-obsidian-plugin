import sidecarVersion from '../../sidecar-version.json';

/** GitHub release tag containing the sidecar required by this plugin build. */
export const REQUIRED_SIDECAR_VERSION = sidecarVersion.version;
